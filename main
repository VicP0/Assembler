#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>
#include "lexer.h"

int main(int argc, char *argv[]) {
usage(argc
if (argc < 2) {
        fprintf(stderr, "Usage: %s file1 file2 ... fileN\n", argv[0]);
        return 1;
    }
for (i = 1; i < argc; i++) {
        FILE* file = fopen(argv[i], "r");
        












}
