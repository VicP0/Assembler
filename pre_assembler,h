#include "macro_list.h"

int pre_assembler(char * file_name);
char*  remove_beginning_spaces(char *line);/*eliminating extra spaces (between chars,before first word and after last word), ensuring that only single spaces remain.*/
char* copy(char *source,char end, char *target);/* copy part(till 'end char') of a string from source to target*/
int word_type(char *pointer, node *head);
void remove_multiple_spaces(char *str);




 
